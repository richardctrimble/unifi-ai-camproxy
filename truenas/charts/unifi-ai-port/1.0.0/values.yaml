# Default values for unifi-ai-port TrueNAS app.
# Overridden by answers from the install wizard (questions.yaml).
# Camera and AI settings are configured via the web UI after install.

image:
  repository: ghcr.io/richardctrimble/unifi-ai-port
  tag: latest
  pullPolicy: IfNotPresent

unifi:
  host: ""
  username: ""
  password: ""

gpu:
  intel: false

web_tool:
  port: 8091

persistence:
  config:
    hostPath: ""
